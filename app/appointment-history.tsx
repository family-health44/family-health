// app/appointment-history.tsx
// Route file only — imports screen from features layer.
// Accessed via router.push('/(app)/appointment-history', { params: { personId, doctorId, ... } })
import { AppointmentHistoryScreen } from '@/features/appointments/screens/AppointmentHistoryScreen';
export default AppointmentHistoryScreen;
